// Powered by Infostretch 

timestamps {

node () {

	stage ('sand - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'PAT_Jenkins', url: 'https://github.com/Harish-410/sand-ball-demo.git']]]) 
	}
	stage ('sand - Build') {
 	
// Unable to convert a build step referring to "org.jenkinsci.plugins.unity3d.Unity3dBuilder". Please verify and convert manually if required. 
	}
}
}